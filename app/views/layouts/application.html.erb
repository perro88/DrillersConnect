<!DOCTYPE html>
<html>
  <head>
    <title>DrillersConnect</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body class="body">
    <nav class="navbar navbar-light bg-light">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">
          <img src="https://external-content.duckduckgo.com/iu/?u=https%3A%2F%2Fwww.drillingcontractor.org%2Fwp-content%2Fuploads%2F2014%2F11%2FEnsign.png&f=1&nofb=1" alt="Drill Rig" width="30" height="24" class="d-inline-block align-text-top">
          DrillersConnect
        </a>
        <%= link_to "Home", root_path %>
        <%= link_to "Jobs", jobs_path %>
        <%= link_to "Users", users_path %>
        <% if user_signed_in? %>
          <% if !current_user.has_role? :employer%>
            <% if user_signed_in? %>
              <%= link_to "Profile", information_index_path %>
            <% end %>
          <% else %>
            <%= link_to "Edit Profile Settings", edit_user_registration_path() %>
          <% end %>
        <% end %>
        <span>
          <% if user_signed_in? %>
            <%= current_user.first_name %>
            |
            <%= link_to 'Logout', destroy_user_session_path, method: :delete %>
          <% else %>
            <%= link_to 'Login', new_user_session_path %>
            |
            <%= link_to 'Sign Up', new_user_registration_path %>
          <% end %>
        </span>
      </div>
    </nav>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <div class="d-flex justify-content-around flex-column align-items-center">
    <%= yield %>
    </div>
  </body>
  <footer>
    <%= link_to "Previous Page", :back %>
  </footer>
</html>
